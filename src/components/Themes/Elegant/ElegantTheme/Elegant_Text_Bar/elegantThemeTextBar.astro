---
import "./elegantThemeTextBar.css";
import type { BlockData } from "../../../../../lib/api/types";
import type { Language } from "../../../../../lib/utils/i18n/translations";
import { FunnelElegantComponents } from "../../../../../lib/constants/themes";

// Get initial language from cookie during SSR
const currentLang: Language = (Astro.cookies.get("lang")?.value || "ar") as Language;
const isArabic = currentLang === "ar";

const data: BlockData = Astro.props.data;
---

<section 
  id={FunnelElegantComponents.ElegantTextBar} 
  class="elegant-section py-12"
>
  <div class="elegant-text-bar-container flex justify-center items-center">
    <div class="elegant-text-bar-animated-button">
      <span class="elegant-text-bar-title">
        {isArabic ? data.title_ar : data.title_en}
      </span>
    </div>
  </div>
</section>