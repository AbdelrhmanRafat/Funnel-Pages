---
import "./troyFaq.css";
import type { BlockData, Faqs } from "../../../../../lib/api/types";
import { FunnelTroyComponents } from "../../../../../lib/constants/themes";

const data: BlockData = Astro.props.data;
const faqs: Faqs[] = data.faqs ?? [];
---

<!-- FAQ Section Component for Astro -->
<section id={FunnelTroyComponents.TroyFaq} class="troy-section">
    <!-- Header -->
    <div class="troy-section-header">
        <i class={data.icon}></i>
        <h5>{data.title}</h5>
    </div>
    <!-- FAQ Items -->
    <faq-accordion class="flex flex-col gap-4">
        {
            faqs.map((faq) => (
                <faq-item class="troy-faq-item overflow-hidden">
                    <button 
                        class="troy-faq-question w-full p-6 flex items-center justify-between bg-transparent border-none cursor-pointer"
                        data-faq-button
                    >
                        <span class="troy-faq-question-text font-medium">
                            {faq.question}
                        </span>
                        <span 
                            class="troy-faq-icon text-xl font-light transition-transform duration-300 ease-in-out rotate-0"
                            data-faq-icon
                        >
                            +
                        </span>
                    </button>
                    <div 
                        class="troy-faq-answer px-6 max-h-0 overflow-hidden"
                        data-faq-answer
                    >
                        <div class="troy-faq-answer-content leading-relaxed py-2">
                            {faq.answer}
                        </div>
                    </div>
                </faq-item>
            ))
        }
    </faq-accordion>
</section>

<script>
    import "./troyFaq";
</script>