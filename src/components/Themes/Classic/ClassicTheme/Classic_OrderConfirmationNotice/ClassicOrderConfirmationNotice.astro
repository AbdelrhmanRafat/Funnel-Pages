---
import type { BlockData } from "../../../../../lib/api/types";

const data: BlockData = Astro.props.data;

---

<div class="bg-gray-100 text-gray-700 rounded-2xl text-center py-4">
  <p class="mb-2">{data.title}</p>
  <div class="flex justify-center items-center gap-8 text-sm text-gray-800">
    {
      data.confirmationNotices.map((item, index) => (
        <div class="flex items-center gap-1">
         <div class="flex justify-center items-center rounded-full w-6 h-6 bg-blue-600 text-white">
          <span>✓</span>
          </div>
          <span>{item}</span>
        </div>
      ))
    }
  </div>
</div>
