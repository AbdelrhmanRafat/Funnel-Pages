---
// This component renders the footer section for the Classic theme.
// It displays information about the store and copyright details.
// Props:
// - data: An object of type BlockData, containing company name and year for the copyright notice.

import type { BlockData } from "../../../../../lib/api/types";
// Defines the expected props for the component.

// Retrieves the data prop passed to the component.
const data: BlockData = Astro.props.data;
---
<footer class="text-center w-full p-14  md:p-24 border-t-2 border-gray-400 mx-auto">
  <div class="flex flex-col justify-center items-center gap-7">
    <!-- Title -->
    <h5 class="text-3xl font-bold text-black">{data.company_name}</h5>

    <!-- Links -->
    <div class="flex justify-center flex-wrap gap-6 text-sm font-medium text-black mb-6">
      {data.policies.map((policy : string) => (
        <a href="#" class="underline">{policy}</a>
      ))}
    </div>

    <!-- Social Icons -->
    <div class="flex justify-center gap-6 text-2xl text-black">
      {data.social_icons.map((iconClass : string, index : number) => (
        <a href="#" aria-label={`social-${index}`}>
          <i class={iconClass}></i>
        </a>
      ))}
    </div>
  </div>
</footer>