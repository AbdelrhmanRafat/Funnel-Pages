---
import type { BlockData, DeliveryFeatures, Features } from "../../../../../lib/api/types";

const data: BlockData = Astro.props.data;
const deliveryFeatures: Features[] = data.features;
---
<section id="classic-delivery-features" class="classic-section">
  <!-- Header -->
  <div class="classic-section-header">
    <i class={data.icon}></i>
    <h5>{data.title}</h5>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
    {
      deliveryFeatures.map((feature) => (
        <div class="p-6 text-center">
          <div class="w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <i class={`${feature.icon} text-blue-600 text-4xl`} />
          </div>
          <h3 class="text-gray-900 font-semibold text-lg mb-2 leading-tight">
            {feature.title}
          </h3>
          <p class="text-gray-600 text-sm leading-relaxed">{feature.subtitle}</p>
        </div>
      ))
    }
  </div>
</section>