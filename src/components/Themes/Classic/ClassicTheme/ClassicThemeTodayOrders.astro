---
import type { BlockData } from "../../../../Interfaces/Products";

interface Props {
  data?: BlockData;
}

const { data } = Astro.props;
---

      <div class="mt-8 bg-gold border-2 border-burgundy rounded-lg p-6">
        <div class="flex items-center justify-center">
          <span class="text-burgundy font-bold font-serif text-lg">طلبات اليوم: </span>
          <span id="todayOrders" class="text-white font-bold mr-3 text-2xl bg-burgundy px-3 py-1 rounded">127</span>
          <span class="text-burgundy font-bold font-serif text-lg">طلب</span>
        </div>
      </div>
    </div>