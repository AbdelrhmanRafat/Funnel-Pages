---
import type { BlockData, Features } from "../../../../../lib/api/types";

const data: BlockData = Astro.props.data;
const productFeatures: Features[] = data.features;
---

<section id="classic-product-features" class="bg-white shadow-2xl p-5 rounded-2xl">
      <!-- Header -->
    <div class="flex justify-start gap-2 items-center mb-6 text-3xl font-bold text-black">
      <i class={data.icon}></i>
      <h5>{data.title}</h5>
    </div>
  <div
    class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6"
  >
    {
      productFeatures.map((feature) => (
        <div class="bg-white rounded-2xl p-6 border border-gray-200 text-center">
          <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <i class={`${feature.icon} text-blue-500 text-2xl`} />
          </div>
          <h3 class="text-gray-900 font-semibold text-lg mb-2">
            {feature.title}
          </h3>
          <p class="text-gray-600 text-sm">{feature.subtitle}</p>
        </div>
      ))
    }
  </div>
</section>
