---
import type { BlockData } from "../../../../../lib/api/types";
import ClassicHeader from "./classic_header/classic_header";
interface Props {
  data?: BlockData;
}

const data = (Astro.props as { data: BlockData }).data;
---

<ClassicHeader headerData={data} client:load />

